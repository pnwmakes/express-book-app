doctype html
html(lang="en")
  head
    title Add New Book
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="/styles.css")

  body
    script.
      function toggleDarkMode() {
        document.documentElement.classList.toggle('dark');
      }

    .container.add-book-page
      button(onclick="toggleDarkMode()" class="btn btn-dark-mode") Toggle Dark Mode

      h1.title Add New Book

      .form-wrapper
        .form-card
          form(action="/add-book", method="POST")
            input(type="text", name="title", placeholder="Title", required)
            input(type="text", name="author", placeholder="Author", required)
            input(type="text", name="isbn", placeholder="ISBN", required)
            input(type="text", name="publisher", placeholder="Publisher (optional)")
            input(type="date", name="published")
            input(type="number", name="pages", placeholder="Page count (optional)")
            textarea(name="description", rows="3", placeholder="Description (optional)")
            input(type="url", name="website", placeholder="Website URL (optional)")
            button(type="submit", class="btn btn-green") Add Book

        .back-link.centered
          a(href="/") ← Back to Book List

    footer.footer Built by James · Victory Code Bootcamp
