doctype html
html
  head
    title Add New Book
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css")
  body(class="bg-gray-100 min-h-screen text-gray-900")
    .max-w-xl.mx-auto.mt-12.px-4
      h1.text-3xl.font-bold.mb-6.text-center Add New Book

      form(action="/add-book" method="POST" class="bg-white shadow-md rounded-lg p-6 space-y-4")
        input(type="text" name="title" placeholder="Title" required class="w-full px-4 py-2 border border-gray-300 rounded-md")
        input(type="text" name="author" placeholder="Author" required class="w-full px-4 py-2 border border-gray-300 rounded-md")
        input(type="text" name="isbn" placeholder="ISBN" required class="w-full px-4 py-2 border border-gray-300 rounded-md")
        input(type="text" name="publisher" placeholder="Publisher (optional)" class="w-full px-4 py-2 border border-gray-300 rounded-md")
        input(type="date" name="published" class="w-full px-4 py-2 border border-gray-300 rounded-md")
        input(type="number" name="pages" placeholder="Page count (optional)" class="w-full px-4 py-2 border border-gray-300 rounded-md")
        textarea(name="description" rows="3" placeholder="Description (optional)" class="w-full px-4 py-2 border border-gray-300 rounded-md")
        input(type="url" name="website" placeholder="Website URL (optional)" class="w-full px-4 py-2 border border-gray-300 rounded-md")

        button(type="submit" class="w-full bg-green-600 text-white py-2 rounded-md hover:bg-green-700 transition") Add Book

      .mt-4.text-center
        a(href="/" class="text-blue-600 hover:underline") ← Back to Book List
