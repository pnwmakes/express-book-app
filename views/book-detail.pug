doctype html
html(lang="en")
  head
    title= book.title
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="/styles.css")
  body
    script.
      if (localStorage.getItem('darkMode') === 'true') {
        document.documentElement.classList.add('dark');
      }
      function toggleDarkMode() {
        const isDark = document.documentElement.classList.toggle('dark');
        localStorage.setItem('darkMode', isDark);
      }

    .container
      button(onclick="toggleDarkMode()" class="btn btn-dark-mode") Toggle Dark Mode

      header
        h1.title= book.title

      .card(style="margin: 0 auto; max-width: 28rem;")
        if book.author
          p
            strong Author:
            |  #{book.author}
        if book.isbn
          p
            strong ISBN:
            |  #{book.isbn}
        if book.publisher
          p
            strong Publisher:
            |  #{book.publisher}
        if book.published
          p
            strong Published:
            |  #{book.published.toDateString()}
        if book.pages
          p
            strong Pages:
            |  #{book.pages}
        if book.description
          p
            strong Description:
            |  #{book.description}
        if book.website
          p
            strong Website:
            a(href=book.website, class="text-blue-600", target="_blank") Visit

      .card-actions(style="margin: 1rem auto; max-width: 28rem; display: flex; justify-content: center; gap: 1rem;")
        a(href="/" class="btn btn-gray") Back to Book List
        a(href=`/edit-book/${book.isbn}` class="btn btn-yellow") Edit
        form(action=`/delete-book/${book.isbn}`, method="POST")
          button(type="submit" class="btn btn-red") Delete

    footer(class="text-center")
      | Built by James · Victory Code Bootcamp
